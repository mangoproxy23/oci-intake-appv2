# OS / editor junk
.DS_Store
~$*

# Python
__pycache__/
.venv/
.env.local

# Node
node_modules

# QA scratch
qa/
*.json.bak

# Uploaded bills / inventory (keep the folder, not its contents)
uploads/*
!uploads/.gitkeep

# Generated / preview workbooks
*.xlsx

# Reference docs and test data (not source)
*.pdf
*.csv

# Local agent / editor config
.claude/

# Networking design artifacts and docs (not part of the app)
*.docx
*.drawio
*.png
*.svg
oci_network_diagram.*
option1_sftpgo.png
option2_soa_mft.png

# Local-only analysis data (not app source)
cost_model.json
data/customer_sites.json
data/oci_di_rest_task_sftp.json
data/oci_resiliency_best_practices.json

# Presentation decks (may be built from a customer's bill)
*.pptx
# Agent scratch notes (may reference customer names)
AGENT_READOUT.md
.vercel

# Oracle Cost Estimator raw snapshot (42 MB, regenerable via
# scripts/refresh_oracle_catalog.py). data/estimator_services.json is the derived file.
oracle-cost-estimator-ai-integration/*.json
