pandas
# Used by the official AWS/Azure/GCP compute-shape table importer.
lxml
openpyxl
xlrd
pypdf
boto3
legacy-cgi; python_version >= "3.13"
# openpyxl needs Pillow to embed ANY image. Without it the Full BOM export dies with
# "You must install Pillow to fetch image objects" — the template puts the Oracle logo
# on every sheet, so it fails long before the architecture diagram. Also used to render
# the diagram PNG itself.
pillow
# pycairo is the preferred architecture PNG renderer, but it needs native Cairo
# headers that Vercel's Python image does not provide. The app falls back to
# Pillow there and still includes the editable .drawio file.
